# This file contains configuration variables used by the CGI scripts

# the path for the database file
db_filename = "./data/data.db"

# a set of all existing sensor names
valid_sensors = {"SENSOR1"}

# the API key (must be the same as used on the sensor node)
api_key = "123"
